Create 3 terminals, one on file WordSwap, one on frontend and one on back end

On the WordSwap terminal run "mongod"
On the frontend terminal run "npm start"
On the backend terminal run "nodemon server.js"

The simply open web browsers on "http://localhost:1313".
